# Backend

Node.js + Express API with MongoDB.

## Getting Started

1. `npm install`
2. Set `MONGODB_URI` in `.env`
3. `npm run dev` (or `npm start` in production)

Endpoints live under `routes/`, controllers in `controllers/`, models in `models/`.

